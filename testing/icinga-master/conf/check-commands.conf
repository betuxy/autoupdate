// Mirrors check-commands.conf from the agent image so the master can validate
// service objects that use command_endpoint. The actual execution happens on the agent.
object CheckCommand "needrestart" {
    command = [ "/usr/lib/nagios/plugins/check_needrestart" ]
}
